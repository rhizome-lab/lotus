[package]
name = "viwo-plugin-ai"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
serde_json = "1.0"
rig-core = "0.3"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
tokio-test = "0.4"
