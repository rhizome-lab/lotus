[package]
name = "lotus-plugin-fs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
mlua = { version = "0.10", features = ["luajit52", "vendored", "serialize"] }

[dev-dependencies]
tempfile = "3.8"
