[package]
name = "rhizome-lotus-plugin-procgen"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rand = "0.8"
rand_xoshiro = "0.6"
noise = "0.9"
mlua = { version = "0.10", features = ["luajit52", "vendored", "serialize"] }

[dev-dependencies]
